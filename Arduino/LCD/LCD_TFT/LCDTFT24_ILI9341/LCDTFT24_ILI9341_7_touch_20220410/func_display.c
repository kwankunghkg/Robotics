/*
#ifndef byte
  #warning // #define byte uint8_t
#endif
*/

/*
char char_array[51]={"test ABC char array\0"};
char *string_pointer = char_array;

void Display_string(char *string_pointer) {
  int l=50; // string_pointer.length(); 
  char ch_buf;

  for(int n=0; n<l; n++) {
    ch_buf = *(string_pointer+n);
    if (ch_buf != 0) 
      Display_char(ch_buf);
    else 
      break;
  }
}
*/
